# Generate two DIFFERENT values with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
CAS_ACCESS_TOKEN=REPLACE_WITH_A_RANDOM_HOST_TOKEN
MOBILE_RELAY_SECRET=REPLACE_WITH_A_DIFFERENT_RANDOM_RELAY_SECRET
MOBILE_RELAY_URL=http://127.0.0.1:8787
CAS_WEB_ORIGIN=http://localhost:8081
CAS_CLOUD_DATABASE=./data/devices.sqlite
HOST=127.0.0.1
PORT=8788
# Optional voice transcription, billed to your own Groq account:
# GROQ_API_KEY=
