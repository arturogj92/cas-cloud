{
  "name": "codeagentswarm-cloudflare-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test test/*.test.js",
    "test:integration": "node --test test/relay.integration.test.js",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.120.0",
    "ws": "^8.18.3"
  },
  "license": "AGPL-3.0-only"
}
